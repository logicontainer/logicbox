services:
  logicbox:
    container_name: logicbox
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    build:
      context: .
      dockerfile: Dockerfile
