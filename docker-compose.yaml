services:
  logicbox:
    container_name: logicbox
    restart: unless-stopped
    ports:
      - "${PORT}:3000"
    environment:
      - NODE_ENV=production
    build:
      context: .
      dockerfile: Dockerfile
